# Migration `20200327081436-remove_user_model`

This migration has been generated by Christofer Flores at 3/27/2020, 8:14:36 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."Book" (
    "code" TEXT NOT NULL DEFAULT '' ,
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "owner" TEXT NOT NULL DEFAULT '' ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' 
) 

CREATE TABLE "quaint"."Question" (
    "book" INTEGER NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "query" TEXT NOT NULL DEFAULT '' ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,FOREIGN KEY ("book") REFERENCES "Book"("id") ON DELETE RESTRICT ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."Answer" (
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "gender" TEXT NOT NULL DEFAULT '' ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "order" INTEGER NOT NULL DEFAULT 0 ,
    "question" INTEGER NOT NULL  ,
    "replier" TEXT NOT NULL DEFAULT '' ,
    "response" TEXT NOT NULL DEFAULT '' ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,FOREIGN KEY ("question") REFERENCES "Question"("id") ON DELETE RESTRICT ON UPDATE CASCADE
) 
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200325213854-create_answer_model..20200327081436-remove_user_model
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url = env("DATABASE_URL")
 }
 generator photonjs {
   provider = "prisma-client-js"
@@ -10,23 +10,13 @@
 // Define your own datamodels here and run `yarn redwood db save` to create
 // migrations for them.
 // TODO: Please remove the following example:
-model User {
-  id    Int     @id @default(autoincrement())
-  email String  @unique
-  name  String
-  password String
-  books Book[]
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-}
 model Book {
   id    Int     @id @default(autoincrement())
-  name  String
   code  String  @default(cuid())
-  owner User
+  owner String
   questions Question[]
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
 }
@@ -43,9 +33,10 @@
 model Answer {
   id    Int     @id @default(autoincrement())
   order Int
   response String
-  replier User
+  replier String
+  gender String
   question Question
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
 }
```


